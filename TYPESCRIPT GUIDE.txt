mkdir typescript-test-sample
cd typescript-test-sample
pnpm init
vim package.json
pnpm add --save-dev jest
pnpm add --save-dev ts-jest
npx ts-jest config: init
    //Add the following section to your package.json:
    ```````
        {
          "scripts": {
            "test": "jest"
          }
        }
    ```````
pnpm add --save-dev tsc typescript
tsc --init
npm run test
pnpm add --save-dev @types/jest
